<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Import Kehadiran - PaySync</title>@include('payflow.partials.styles')</head><body><main style="max-width:760px;margin:0 auto;padding:32px 20px;">@include('payflow.partials.brand')<section class="card" style="margin-top:24px;"><h1>Import Kehadiran {{ $payroll->period_label }}</h1>@if(session('import_errors'))<div class="card" style="padding:12px;margin:12px 0;border-left:4px solid var(--red);"><strong>Import gagal, tidak ada data disimpan.</strong>@foreach(session('import_errors') as $error)<p class="muted">Baris {{ $error['row'] }} / {{ $error['field'] }}: {{ $error['message'] }}</p>@endforeach</div>@endif<form method="POST" action="{{ route('payroll.attendance.store',$payroll) }}" enctype="multipart/form-data" class="form-stack">@csrf<label class="field"><span>CSV Attendance</span><input class="input" type="file" name="file" accept=".csv,text/csv" required></label><div><button class="btn btn-primary">Import</button><a class="btn btn-secondary" href="{{ route('payroll.attendance.template') }}">Download Template</a><a class="btn btn-secondary" href="{{ route('payroll.show',$payroll) }}">Kembali</a></div></form></section></main></body></html>
